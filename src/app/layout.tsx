import type { Metadata } from "next";
import { Geist, Geist_Mono, Poppins } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  variable: "--font-poppins",
});

export const metadata: Metadata = {
  title: "Ticket Central | Book Concerts, Events, Parties & Festival Tickets",
  description:
    "Discover and book tickets for concerts, parties, festivals, nightlife events, and live shows near you. Ticket Central makes event ticketing fast, secure, and easy.",
  keywords: [
    "event tickets",
    "concert tickets",
    "party tickets",
    "festival tickets",
    "book events online",
    "events near me",
    "live shows tickets",
    "nightlife events",
    "ticket booking platform",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} ${poppins.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}